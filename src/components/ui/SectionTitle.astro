---
const { title, subtitle } = Astro.props;
---

<div class="mb-10 text-center">
  <h2 class="text-3xl md:text-4xl font-display font-semibold mb-4">
    {title}
  </h2>
  {subtitle && (
    <p class="text-brilint-muted max-w-2xl mx-auto">
      {subtitle}
    </p>
  )}
</div>
