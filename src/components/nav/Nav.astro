---
import Container from '../ui/Container.astro';
import Button from '../ui/Button.astro';
import { WHATSAPP_URL } from '../../config/contact';

const links = [
  { href: '#servicios', label: 'Servicios' },
  { href: '#paquetes', label: 'Paquetes' },
  { href: '#proceso', label: 'Proceso' },
  { href: '#portfolio', label: 'Portafolio' },
  { href: '#testimonios', label: 'Testimonios' },
  { href: '#faq', label: 'FAQ' },
];
---

<header class="fixed top-0 inset-x-0 z-30 border-b border-dab-border/60 backdrop-blur-md bg-dab-bg/80">
  <Container className="flex h-16 items-center justify-between px-1 sm:px-0">
    <a href="#top" class="inline-flex items-center gap-2">
      <span class="h-2 w-2 rounded-full bg-dab-accent"></span>
      <span class="font-body font-bold text-lg">
        Dab
      </span>
    </a>

    <!-- NAV ESCRITORIO -->
    <nav class="font-body hidden md:flex items-center gap-8 text-sm font-medium text-dab-muted">
      {links.map((link) => (
        <a
          href={link.href}
          class="hover:text-dab-text transition duration-fast rounded-full px-2 py-1"
        >
          {link.label}
        </a>
      ))}
    </nav>

    <!-- CTA ESCRITORIO + MENÚ MÓVIL -->
    <div class="flex items-center gap-3">
      <div class="hidden md:block">
        <Button
          href={WHATSAPP_URL("Hola, me interesa una cotización con Dab")}
          variant="primary"
        >
          Hablar por WhatsApp
        </Button>
      </div>

      <!-- MENÚ MÓVIL -->
      <details class="relative md:hidden">
        <summary
          class="list-none inline-flex h-10 w-10 items-center justify-center rounded-full border border-dab-border bg-dab-surface/90 text-dab-text cursor-pointer shadow-dab-soft"
          aria-label="Abrir menú de navegación"
        >
          <span class="flex flex-col gap-1.5">
            <span class="h-0.5 w-4 rounded bg-dab-text"></span>
            <span class="h-0.5 w-4 rounded bg-dab-text"></span>
          </span>
        </summary>

        <div
          class="absolute right-0 mt-3 w-[min(88vw,17rem)] rounded-2xl border border-dab-border bg-dab-bg/95 shadow-lg shadow-black/40 p-3"
        >
          <nav class="font-body flex flex-col">
            {links.map((link) => (
              <a
                href={link.href}
                class="px-4 py-2.5 text-sm font-medium text-dab-muted hover:text-dab-text hover:bg-dab-surface/70 transition rounded-xl"
              >
                {link.label}
              </a>
            ))}

            <div class="mt-2 border-t border-dab-border/60 pt-2">
              <a
                href={WHATSAPP_URL("Hola, me interesa una cotización con Dab")}
                class="mx-1 mt-1 inline-flex w-[calc(100%-0.5rem)] items-center justify-center rounded-lg bg-dab-accent px-3 py-2 text-xs font-semibold text-dab-bg hover:bg-dab-accent/90 transition"
              >
                Hablar por WhatsApp
              </a>
            </div>
          </nav>
        </div>
      </details>
    </div>
  </Container>
</header>
